spring:
  application:
    name: cloud-security
  mail:
    #配置smtp服务主机地址
    host: smtp.163.com
    port: 465
    protocol: smtps
    #发送者邮箱
    username: ${MAIL_USERNAME}
    #配置密码,注意不是真正的密码，而是刚刚申请到的授权码
    password: ${MAIL_KEY}
    properties:
      mail:
        smtp:
          auth: true # 启用身份验证
          ssl:
            enabled: true
            smtp:
              auth: true
            socketFactory:
              class: javax.net.ssl.SSLSocketFactory
              fallback: false
            starttls:
              enable: true
    sender-name: 零雨 # 自定义发件人名称
jwt:
  secret: ${JWT_SECRET}
  expiration: 86400  # 24小时
